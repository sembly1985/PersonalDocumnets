TestSource
	FullName: auto find the whole path of a file
	split: split a string by a charactor
	GetInput: get a string input by stdin, and split it to command string and parameters
	string baseFold: base fold for function FullName